import React from 'react';

const Admin = () => {
    // Проверка на наличие прав администратора
    const isAdmin = false; // Заменить на реальную проверку прав администратора

    if (!isAdmin) {
        return <div>У вас недостаточно прав для доступа к этой странице.</div>;
    }

    return (
        <div>
            <h1>Администраторская панель</h1>
            {/* Здесь будет содержимое панели администратора */}
        </div>
    );
};

export default Admin;
